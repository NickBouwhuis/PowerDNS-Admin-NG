version: "3"

services:
  app:
    image: powerdnsadmin/powerdns-adminng:latest
    container_name: powerdns_adminng
    ports:
      - "9191:3000"
    logging:
      driver: json-file
      options:
        max-size: 50m
    environment:
      - SQLALCHEMY_DATABASE_URI=mysql://pda:changeme@host.docker.internal/pda
      - GUNICORN_TIMEOUT=60
      - GUNICORN_WORKERS=2
      - GUNICORN_LOGLEVEL=DEBUG
