"""
FastAPI web layer — middleware, session management, and server-side callbacks.

The SPA (Next.js) handles all UI rendering. This module provides:
- Server-side session middleware
- Security headers and maintenance mode middleware
- OAuth/SAML callback routes
- DynDNS endpoints
- Health check
"""
